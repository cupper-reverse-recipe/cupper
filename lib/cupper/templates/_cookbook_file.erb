# Cookbook_file Session

<% for @cbf in @files %>
cookbook_file '<%= @cbf.path %>'
  source '<%= @cbf.source %>'
  owner '<%= @cbf.owner %>'
  group '<%= @cbf.group %>'
  mode '<%= @cbf.mode %>'
end
<% end %>
